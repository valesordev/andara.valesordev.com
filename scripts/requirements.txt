# Python dependencies of the scripts under scripts/ beyond the standard library.
# Installed by `make bootstrap` when missing; pinned so CI and a developer machine agree.
#
# PyYAML, because scripts/helm_test.py has to read Kubernetes manifests rendered by helm,
# which are real YAML. The repo's own declaration files (topics.yaml, schemas.yaml, story
# frontmatter) keep their narrow stdlib parsers on purpose.
PyYAML==6.0.3
# reuse verifies REUSE.toml and LICENSES/ for `make license-check` (LICENSING.md).
reuse==6.2.0
# lark parses docs/specs/content-language/v1/grammar.ebnf for `make content-grammar-check`
# (AW-CLI-005 AC-1): the Content Language grammar is checked by a parser generator rather
# than by reading, which is the only one of that story's criteria that does not need the
# compiler AW-CLI-006 has yet to write.
lark==1.2.2

// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 Valesor Development

// Template Definitions — Game Types in a single-inheritance hierarchy that
// contain Components (ADR-0010), as the compiler emits them and the loader
// reads them (AW-SRV-022).
//
// Resolution happens at compile time (ADR-0010 decision 9): the compiler
// walks `extends`, merges Components field by field, and publishes the
// flattened result with the chain and per-field provenance retained. The
// server carries no resolver — a TemplateDefinition with resolved=false is
// rejected at load, never resolved late.
//
// One blob per declaration, at `templates/<name>.json` in the pack. Like a
// ZoneDefinition it rides inside a Blob as opaque bytes, so no registry
// subject names it.

// @generated by protoc-gen-es v2.14.1
// @generated from file andara/content/v1/template.proto (package andara.content.v1, syntax proto3)
/* eslint-disable */

import type { GenEnum, GenFile, GenMessage } from "@bufbuild/protobuf/codegenv2";
import type { Message } from "@bufbuild/protobuf";
import type { ComponentValue } from "./zone_pb";

/**
 * Describes the file andara/content/v1/template.proto.
 */
export declare const file_andara_content_v1_template: GenFile;

/**
 * @generated from message andara.content.v1.TemplateDefinition
 */
export declare type TemplateDefinition = Message<"andara.content.v1.TemplateDefinition"> & {
  /**
   * "<pack>.<Name>": the pack is everything before the last dot, and the
   * name is unique within it. The loader resolves ancestors within the same
   * pack or the core pack and nowhere else.
   *
   * @generated from field: string name = 1;
   */
  name: string;

  /**
   * Open by construction: Brian flagged the kind set as incomplete
   * (ADR-0010 decision 1). Every Template in a chain has the same kind.
   *
   * @generated from field: andara.content.v1.TemplateKind kind = 2;
   */
  kind: TemplateKind;

  /**
   * Root first, self last, compiler-emitted. A root Template's chain is
   * [self]. The loader checks that every ancestor exists, that each
   * Template's chain is its parent's chain plus itself, and that the depth
   * is within MaxChainDepth — a chain that fails any of these is a compiler
   * bug surfaced as a load error rather than a silently wrong Template.
   *
   * @generated from field: repeated string chain = 3;
   */
  chain: string[];

  /**
   * The flattened Component set, sorted by type, at most one of each type
   * (ADR-0010 decision 3). A subtype may override and extend but never
   * remove (decision 5), so every ancestor's Components — and their fields —
   * are present here.
   *
   * @generated from field: repeated andara.content.v1.ComponentValue components = 4;
   */
  components: ComponentValue[];

  /**
   * Which ancestor set each field's value. Sorted by (component, field).
   * "Why does this Template have Aggro{threshold: 3}" is a question about a
   * value that may appear in none of the files a Builder wrote.
   *
   * @generated from field: repeated andara.content.v1.FieldProvenance provenance = 5;
   */
  provenance: FieldProvenance[];

  /**
   * Must be true to load (ADR-0010 decision 9).
   *
   * @generated from field: bool resolved = 6;
   */
  resolved: boolean;

  /**
   * Where the declaration came from, for errors.
   *
   * @generated from field: andara.content.v1.SourceRef source = 7;
   */
  source?: SourceRef | undefined;

  /**
   * Rejected at load with both the file's version and the supported range
   * named, like ZoneDefinition.format_version. Templates are their own
   * blobs, so they version separately from Zones.
   *
   * @generated from field: uint32 format_version = 8;
   */
  formatVersion: number;
};

/**
 * Describes the message andara.content.v1.TemplateDefinition.
 * Use `create(TemplateDefinitionSchema)` to create a new message.
 */
export declare const TemplateDefinitionSchema: GenMessage<TemplateDefinition>;

/**
 * @generated from message andara.content.v1.FieldProvenance
 */
export declare type FieldProvenance = Message<"andara.content.v1.FieldProvenance"> & {
  /**
   * @generated from field: string component = 1;
   */
  component: string;

  /**
   * @generated from field: string field = 2;
   */
  field: string;

  /**
   * The Template in the chain that set this value — self, for a value
   * declared here.
   *
   * @generated from field: string from = 3;
   */
  from: string;
};

/**
 * Describes the message andara.content.v1.FieldProvenance.
 * Use `create(FieldProvenanceSchema)` to create a new message.
 */
export declare const FieldProvenanceSchema: GenMessage<FieldProvenance>;

/**
 * @generated from message andara.content.v1.SourceRef
 */
export declare type SourceRef = Message<"andara.content.v1.SourceRef"> & {
  /**
   * @generated from field: string file = 1;
   */
  file: string;

  /**
   * @generated from field: uint32 line = 2;
   */
  line: number;
};

/**
 * Describes the message andara.content.v1.SourceRef.
 * Use `create(SourceRefSchema)` to create a new message.
 */
export declare const SourceRefSchema: GenMessage<SourceRef>;

/**
 * @generated from enum andara.content.v1.TemplateKind
 */
export enum TemplateKind {
  /**
   * @generated from enum value: TEMPLATE_KIND_UNSPECIFIED = 0;
   */
  TEMPLATE_KIND_UNSPECIFIED = 0,

  /**
   * @generated from enum value: ENTITY = 1;
   */
  ENTITY = 1,

  /**
   * @generated from enum value: ITEM = 2;
   */
  ITEM = 2,

  /**
   * @generated from enum value: BEHAVIOR = 3;
   */
  BEHAVIOR = 3,
}

/**
 * Describes the enum andara.content.v1.TemplateKind.
 */
export declare const TemplateKindSchema: GenEnum<TemplateKind>;

